---
deployment:
  tasks:
    - /bin/echo "Deploying Flowpost Studio from the repository root."
    - /usr/bin/env node scripts/cleanup-cpanel-sample.js
    - /bin/mkdir -p tmp
    - /bin/touch tmp/restart.txt
